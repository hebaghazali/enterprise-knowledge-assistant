# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Config & validation
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.0

# Database
sqlalchemy[asyncio]>=2.0.0
alembic>=1.13.0
psycopg[binary]>=3.1.0

# File handling
python-multipart>=0.0.9
pypdf>=4.0.0

# Vector store
chromadb>=0.5.0

# HTTP client (Ollama calls)
httpx>=0.27.0

# ML / embeddings
# torch is pre-installed by the Dockerfile with platform-specific logic (see TARGETARCH step).
# This entry covers local `pip install -r requirements.txt` and ensures pip won't downgrade
# a Docker-installed wheel that already satisfies this constraint.
torch>=2.4.1,<3.0.0
transformers>=4.40.0
sentence-transformers>=2.7.0
